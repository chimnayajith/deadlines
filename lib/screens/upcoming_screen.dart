// settings_page.dart
import 'package:flutter/material.dart';

class UpcomingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Upcoming')),
      body: Center(child: Text('Upcoming Page')),
    );
  }
}
